#ifndef MERGE_SORT_H
#define MERGE_SORT_H

#include <stddef.h>

/* Function prototypes */
void merge_sort(int *array, size_t size);
void print_array(const int *array, size_t size);

#endif /* MERGE_SORT_H */
